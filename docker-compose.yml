services:
  api_1:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: AIA_1
    network_mode: "host"
    env_file:
      - .env
    environment:
      - PYTHONPATH=/code/app
    volumes:
      - ./src:/code/app
    restart: unless-stopped